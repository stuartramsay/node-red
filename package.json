{
    "name": "Node-Red",
    "description": "For syncing node red from a primary VM to a secondary VM",
    "version": "0.0.1",
    "dependencies": {},
    "node-red": {
        "settings": {
            "flowFile": "flows.json",
            "credentialsFile": "flows_cred.json"
        }
    }
}